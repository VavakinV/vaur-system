ИНФОРМАЦИОННАЯ СИСТЕМА ВНЕАУДИТОРНОЙ РАБОТЫ ПРЕПОДАВАТЕЛЕЙ И СТУДЕНТОВ

## Запуск бэкенда

1. Скопировать файл .env.example в корне ./backend

```bash
cp .env.example .env
```

2. Запуск контейнеров:

```bash
docker compose up --build
```

Бэкенд будет доступен по ссылке `http://localhost:8000`.
